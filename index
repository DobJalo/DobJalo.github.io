<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Game Dev Portfolio</title>
  <style>
    :root {
      --main-color: #cc0000;
      --text-color: #ffffff;
      --bg-color: #1a1a1a;
      --transition: 0.4s ease;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      scroll-behavior: smooth;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: var(--bg-color);
      color: var(--text-color);
    }

    header {
      background-color: var(--main-color);
      padding: 1rem 2rem;
      position: sticky;
      top: 0;
      z-index: 100;
    }

    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
    }

    nav h1 {
      font-size: 1.5rem;
    }

    .nav-links a {
      margin-left: 1.5rem;
      color: var(--text-color);
      text-decoration: none;
      font-weight: bold;
      transition: color var(--transition);
    }

    .nav-links a:hover {
      color: #ffaaaa;
    }

    section {
      padding: 4rem 2rem;
      transition: opacity var(--transition), transform var(--transition);
    }

    h2 {
      color: var(--main-color);
      margin-bottom: 1rem;
    }

    .container {
      max-width: 900px;
      margin: auto;
    }

    /* Адаптивность */
    @media (max-width: 768px) {
      nav {
        flex-direction: column;
        align-items: flex-start;
      }

      .nav-links {
        display: flex;
        flex-direction: column;
        width: 100%;
        margin-top: 1rem;
      }

      .nav-links a {
        margin: 0.5rem 0;
      }
    }
  </style>
</head>
<body>
  <header>
    <nav>
      <h1>My GameDev Portfolio</h1>
      <div class="nav-links">
        <a href="#about">About me</a>
        <a href="#games">Games</a>
        <a href="#art">Art</a>
        <a href="#contact">Contact</a>
      </div>
    </nav>
  </header>

  <main class="container">
    <section id="about">
      <h2>About Me</h2>
      <p>Я — разработчица игр, увлечённая созданием уникального игрового опыта и визуального стиля.</p>
    </section>

    <section id="games">
      <h2>Games</h2>
      <p>Здесь будут мои проекты.</p>
    </section>

    <section id="art">
      <h2>Art</h2>
      <p>Здесь будет арт.</p>
    </section>

    <section id="contact">
      <h2>Contact</h2>
      <p>Связаться со мной можно через email или соцсети.</p>
    </section>
  </main>
</body>
</html>
